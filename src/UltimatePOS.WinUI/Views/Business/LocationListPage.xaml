<Page
    x:Class="UltimatePOS.WinUI.Views.Business.LocationListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:UltimatePOS.Core.ViewModels.Business"
    xmlns:entities="using:UltimatePOS.Core.Entities"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Spacing="4">
                <TextBlock Text="Locations" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="Manage warehouses and stores for current business" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    Content="Add Location" 
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.AddLocationCommand}"
                    IsEnabled="{x:Bind ViewModel.CanAddLocation, Mode=OneWay}"/>
        </Grid>

        <!-- List -->
        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.Locations, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedLocation, Mode=TwoWay}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entities:Location">
                    <Grid Padding="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon/Logo Placeholder -->
                        <Border Width="40" Height="40" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0,0,16,0">
                            <FontIcon Glyph="&#xE81D;" FontSize="20"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock Text="{x:Bind Address}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Loading -->
        <ProgressRing Grid.Row="1" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" 
                      HorizontalAlignment="Center" VerticalAlignment="Center" Width="40" Height="40"/>
    </Grid>
</Page>
