<Page
    x:Class="UltimatePOS.WinUI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:UltimatePOS.Core.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="{x:Bind ViewModel.WelcomeMessage, Mode=OneWay}" 
                       Style="{StaticResource TitleTextBlockStyle}"/>

            <Button Grid.Column="1" 
                    Content="Logout" 
                    Command="{x:Bind ViewModel.LogoutCommand}"/>
        </Grid>

        <!-- Navigation Cards -->
        <GridView Grid.Row="1" SelectionMode="None">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="0,0,12,12"/>
                </Style>
            </GridView.ItemContainerStyle>

            <!-- Businesses Card -->
            <Button Command="{x:Bind ViewModel.NavigateToBusinessesCommand}" 
                    Padding="0" BorderThickness="0" Background="Transparent">
                <Grid Width="200" Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                      CornerRadius="8" Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <FontIcon Glyph="&#xE719;" FontSize="32" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Text="Businesses" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </Grid>
            </Button>

            <!-- Locations Card -->
            <Button Command="{x:Bind ViewModel.NavigateToLocationsCommand}" 
                    Padding="0" BorderThickness="0" Background="Transparent">
                <Grid Width="200" Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                      CornerRadius="8" Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <FontIcon Glyph="&#xE81D;" FontSize="32" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Text="Locations" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </Grid>
            </Button>

            <!-- Products Card -->
            <Button Command="{x:Bind ViewModel.NavigateToProductsCommand}" 
                    Padding="0" BorderThickness="0" Background="Transparent">
                <Grid Width="200" Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                      CornerRadius="8" Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <FontIcon Glyph="&#xE781;" FontSize="32" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Text="Products" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </Grid>
            </Button>

            <!-- Stock Card -->
            <Button Command="{x:Bind ViewModel.NavigateToStockCommand}" 
                    Padding="0" BorderThickness="0" Background="Transparent">
                <Grid Width="200" Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                      CornerRadius="8" Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <FontIcon Glyph="&#xE74C;" FontSize="32" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Text="Stock" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </Grid>
            </Button>
        </GridView>
    </Grid>
</Page>
