<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UltimatePOS.WinUI.Views.Stock.StockListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UltimatePOS.WinUI.Views.Stock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:UltimatePOS.Core.ViewModels.Stock"
    xmlns:converters="using:UltimatePOS.WinUI.Converters"
    xmlns:ctWinUI="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    x:Name="PageRoot">

    <Page.Resources>

        <converters:EmptyCountToVisibilityConverter x:Key="EmptyCountToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Actions -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Spacing="4">
                <TextBlock Text="Stock Management" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="Manage inventory levels, adjustments and transfers" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.6"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Command="{x:Bind ViewModel.ManageReorderLevelsCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B7;"/>
                        <TextBlock Text="Manage Reorder Levels"/>
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.TransferStockCommand}" Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8D5;"/>
                        <TextBlock Text="Transfer Stock"/>
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.LoadDataCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Filters and Content -->
        <Pivot Grid.Row="1">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="16" FontWeight="SemiBold"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            
            <PivotItem Header="Current Stock">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Filters -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,16">
                        <ComboBox Header="Location" 
                                  ItemsSource="{x:Bind ViewModel.Locations, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedLocation, Mode=TwoWay}"
                                  DisplayMemberPath="Name" 
                                  MinWidth="200"
                                  PlaceholderText="Select Location"/>

                        <CheckBox Content="Show Low Stock Only" 
                                  IsChecked="{x:Bind ViewModel.ShowLowStockOnly, Mode=TwoWay}"
                                  Margin="0,28,0,0"/>
                    </StackPanel>

                    <!-- DataGrid -->
                    <ctWinUI:DataGrid Grid.Row="1"
                                      ItemsSource="{x:Bind ViewModel.Stocks, Mode=OneWay}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      GridLinesVisibility="Horizontal"
                                      BorderThickness="1"
                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                      CornerRadius="4">
                        <ctWinUI:DataGrid.Columns>
                            <ctWinUI:DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*"/>
                            <ctWinUI:DataGridTextColumn Header="SKU" Binding="{Binding Product.SKU}" Width="150"/>
                            <ctWinUI:DataGridTextColumn Header="Location" Binding="{Binding Location.Name}" Width="150"/>
                            <ctWinUI:DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                            <ctWinUI:DataGridTextColumn Header="Alert Level" Binding="{Binding Product.AlertQuantity}" Width="100"/>
                            <ctWinUI:DataGridTemplateColumn Header="Actions" Width="Auto">
                                <ctWinUI:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <Button ToolTipService.ToolTip="Adjust Stock" 
                                                    Command="{Binding DataContext.AdjustStockCommand, ElementName=PageRoot}"
                                                    CommandParameter="{Binding}">
                                                <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                                            </Button>
                                            <Button ToolTipService.ToolTip="History" 
                                                    Command="{Binding DataContext.ViewHistoryCommand, ElementName=PageRoot}"
                                                    CommandParameter="{Binding}">
                                                <FontIcon Glyph="&#xE81C;" FontSize="14"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </ctWinUI:DataGridTemplateColumn.CellTemplate>
                            </ctWinUI:DataGridTemplateColumn>
                        </ctWinUI:DataGrid.Columns>
                    </ctWinUI:DataGrid>
                    
                    <!-- Empty State -->
                    <StackPanel Grid.Row="1" 
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Spacing="12"
                                Visibility="{x:Bind ViewModel.Stocks.Count, Converter={StaticResource EmptyCountToVisibilityConverter}, Mode=OneWay}">
                        <!-- Only show if not loading -->
                        <StackPanel Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}, Mode=OneWay}">
                            <FontIcon Glyph="&#xE9ce;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="No stock records found" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Select a different location or remove filters" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </PivotItem>
            
            <PivotItem Header="Stock Takes">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Action Bar -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,16">
                        <Button Command="{x:Bind ViewModel.NewStockTakeCommand}" Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE710;"/>
                                <TextBlock Text="New Stock Take"/>
                            </StackPanel>
                        </Button>
                        
                        <ComboBox Header="Location" 
                                  ItemsSource="{x:Bind ViewModel.Locations, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedLocation, Mode=TwoWay}"
                                  DisplayMemberPath="Name" 
                                  MinWidth="200"
                                  PlaceholderText="Filter by Location"/>
                    </StackPanel>
                    
                    <!-- DataGrid -->
                    <ctWinUI:DataGrid Grid.Row="1"
                                      ItemsSource="{x:Bind ViewModel.StockTakes, Mode=OneWay}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      GridLinesVisibility="Horizontal"
                                      BorderThickness="1"
                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                      CornerRadius="4">
                        <ctWinUI:DataGrid.Columns>
                            <ctWinUI:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                            <ctWinUI:DataGridTextColumn Header="Date" Binding="{Binding StockTakeDate, Converter={StaticResource DateTimeToStringConverter}}" Width="150"/>
                            <ctWinUI:DataGridTextColumn Header="Location" Binding="{Binding Location.Name}" Width="150"/>
                            <ctWinUI:DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                            <ctWinUI:DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                            <ctWinUI:DataGridTemplateColumn Header="Actions" Width="Auto">
                                <ctWinUI:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button ToolTipService.ToolTip="Open Stock Take" 
                                                Command="{Binding DataContext.OpenStockTakeCommand, ElementName=PageRoot}"
                                                CommandParameter="{Binding}">
                                            <FontIcon Glyph="&#xE8A7;" FontSize="14"/>
                                        </Button>
                                    </DataTemplate>
                                </ctWinUI:DataGridTemplateColumn.CellTemplate>
                            </ctWinUI:DataGridTemplateColumn>
                        </ctWinUI:DataGrid.Columns>
                    </ctWinUI:DataGrid>
                    
                    <!-- Empty State -->
                    <StackPanel Grid.Row="1" 
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Spacing="12"
                                Visibility="{x:Bind ViewModel.StockTakes.Count, Converter={StaticResource EmptyCountToVisibilityConverter}, Mode=OneWay}">
                        <!-- Only show if not loading -->
                        <StackPanel Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}, Mode=OneWay}">
                            <FontIcon Glyph="&#xE9ce;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="No stock takes found" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Create a new stock take to get started" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </PivotItem>
        </Pivot>

        <!-- Loading Indicator (Global) -->
        <ProgressRing Grid.RowSpan="2" 
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      Width="50" Height="50"
                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
