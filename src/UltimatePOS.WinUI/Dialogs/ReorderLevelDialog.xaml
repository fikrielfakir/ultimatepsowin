&lt;?xml version="1.0" encoding="utf-8"?&gt;
&lt;ContentDialog
    x:Class="UltimatePOS.WinUI.Dialogs.ReorderLevelDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UltimatePOS.WinUI.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:UltimatePOS.Core.ViewModels.Stock"
    xmlns:converters="using:UltimatePOS.WinUI.Converters"
    xmlns:ctWinUI="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="Manage Reorder Levels"
    PrimaryButtonText="Save"
    PrimaryButtonCommand="{x:Bind ViewModel.SaveCommand}"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}"&gt;

    &lt;ContentDialog.Resources&gt;
        &lt;converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/&gt;
    &lt;/ContentDialog.Resources&gt;

    &lt;Grid MinWidth="800" MinHeight="600"&gt;
        &lt;Grid.RowDefinitions&gt;
            &lt;RowDefinition Height="Auto"/&gt;
            &lt;RowDefinition Height="*"/&gt;
        &lt;/Grid.RowDefinitions&gt;

        &lt;!-- Filters --&gt;
        &lt;StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,16"&gt;
            &lt;TextBlock Text="Set alert quantities for products. You'll be notified when stock falls below these levels." 
                       Style="{StaticResource BodyTextBlockStyle}" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/&gt;

            &lt;StackPanel Orientation="Horizontal" Spacing="16"&gt;
                &lt;ComboBox Header="Location" 
                          ItemsSource="{x:Bind ViewModel.Locations, Mode=OneWay}"
                          SelectedItem="{x:Bind ViewModel.SelectedLocation, Mode=TwoWay}"
                          DisplayMemberPath="Name" 
                          MinWidth="200"/&gt;

                &lt;TextBox Header="Search Products" 
                         Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         PlaceholderText="Search by name or SKU"
                         MinWidth="250"/&gt;

                &lt;Button Command="{x:Bind ViewModel.LoadProductsCommand}" 
                        VerticalAlignment="Bottom"&gt;
                    &lt;StackPanel Orientation="Horizontal" Spacing="8"&gt;
                        &lt;FontIcon Glyph="&amp;#xE72C;" FontSize="14"/&gt;
                        &lt;TextBlock Text="Refresh"/&gt;
                    &lt;/StackPanel&gt;
                &lt;/Button&gt;
            &lt;/StackPanel&gt;
        &lt;/StackPanel&gt;

        &lt;!-- Products DataGrid --&gt;
        &lt;Grid Grid.Row="1"&gt;
            &lt;ctWinUI:DataGrid ItemsSource="{x:Bind ViewModel.Products, Mode=OneWay}"
                              AutoGenerateColumns="False"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="1"
                              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                              CornerRadius="4"&gt;
                &lt;ctWinUI:DataGrid.Columns&gt;
                    &lt;ctWinUI:DataGridTextColumn Header="Product Name" 
                                                Binding="{Binding Name}" 
                                                Width="*"
                                                IsReadOnly="True"/&gt;
                    &lt;ctWinUI:DataGridTextColumn Header="SKU" 
                                                Binding="{Binding SKU}" 
                                                Width="120"
                                                IsReadOnly="True"/&gt;
                    &lt;ctWinUI:DataGridTextColumn Header="Category" 
                                                Binding="{Binding Category.Name}" 
                                                Width="150"
                                                IsReadOnly="True"/&gt;
                    &lt;ctWinUI:DataGridTemplateColumn Header="Alert Quantity" Width="150"&gt;
                        &lt;ctWinUI:DataGridTemplateColumn.CellTemplate&gt;
                            &lt;DataTemplate&gt;
                                &lt;NumberBox Value="{Binding AlertQuantity, Mode=TwoWay}" 
                                           SpinButtonPlacementMode="Inline"
                                           Minimum="0"
                                           ValueChanged="OnAlertQuantityChanged"
                                           HorizontalAlignment="Stretch"
                                           Margin="4,0"/&gt;
                            &lt;/DataTemplate&gt;
                        &lt;/ctWinUI:DataGridTemplateColumn.CellTemplate&gt;
                    &lt;/ctWinUI:DataGridTemplateColumn&gt;
                &lt;/ctWinUI:DataGrid.Columns&gt;
            &lt;/ctWinUI:DataGrid&gt;

            &lt;!-- Loading Indicator --&gt;
            &lt;ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Width="50" Height="50"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/&gt;
        &lt;/Grid&gt;
    &lt;/Grid&gt;
&lt;/ContentDialog&gt;
